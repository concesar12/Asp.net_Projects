<div class="list-container">
    @{
        ViewBag.ListTitle = "Updated";
    }
    <h3>@ViewBag.ListTitle</h3>
    <ul class="list">
        @foreach (string barrio in ViewBag.ListItems)
        {
            <li>@barrio</li>
        }
    </ul>
</div>